<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="">
    </head>
    <body>
        <h1>This is the user show page</h1>
        
        <!-- DISPLAYS MESSAGE IF NO TRIPS -->
        <%if(user.trips.length === 0){%>
            <h2>There are no trips yet</h2>
        <!-- FUNCTION TO ADD TRIPS TO PAGE FOR SPECIFIC USER -->
        <%}else{%>
            <ul class="trips">
                <%user.trips.forEach((trip)=>{%>
                        <!-- LISTS USERS TRIPS BY CITY ONLY HREF TIED TO TRIP SHOW ROUTE -->
                    <li><a href="/trips/<%=trip._id%>"><%=trip.destinationCity%></a></li>
                    <%})%>
            </ul>
        <%}%>
        
        <!-- BELOW NEEDS TO LINK TO SHOW FORM TO CREATE TRIP PAGE -->
        <a href="/trips/new/<%=user._id%>"><button class="create-trip-button">Create a Trip!</button></a> 
        <a href="/users/<%=user._id%>/edit"><button>Edit Account</button></a>
        <a href="/users/logout"><button class="logout-button">Logout</button></a>
        <script src="" async defer></script>
    </body>
</html>